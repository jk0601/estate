# 사용법 · 수정방법

이 저장소에는 **세 개의 도구**가 있습니다. (화면 위쪽 메뉴로 서로 이동)

| 도구 | 화면 | 하는 일 |
|---|---|---|
| **① 입주예정 단지 목록** | `docs/index.html` | 수도권(서울·경기) 입주예정 단지 + 분양가를 한눈에 |
| **② 매매·전세 동향 추적기** | `docs/watch.html` | 관심 단지의 매물을 매일 수집해 가격 변동·프리미엄을 표·그래프로 |
| **③ 8억 레이더** | `docs/radar.html` | 서울 10년 내외 아파트를 **실거래·경매·공매** 세 갈래로 매일 확인 |

**목차**
- [네이버 부동산과 뭐가 다른가요?](#네이버-부동산과-뭐가-다른가요)
- [① 입주예정 단지 목록](#-입주예정-단지-목록-docsindexhtml)
- [② 매매·전세 동향 추적기](#-매매전세-동향-추적기-docswatchhtml)
- [③ 8억 레이더](#-8억-레이더-docsradarhtml)
- [가족에게 공유하기 (GitHub Pages)](#가족에게-공유하기-github-pages)
- [공통 주의사항](#공통-주의사항)

---

## 네이버 부동산과 뭐가 다른가요?

- **네이버 부동산**: 단지 이름을 이미 알아야 검색됩니다. "앞으로 어디에 입주하는지" 목록은 없습니다.
- **이 도구**: 앞으로 수도권에 입주하는 단지 **목록 + 원래 분양가**를 자동으로 모아
  **"어디를 봐야 할지"**를 알려줍니다. 실제 현재 호가는 **'매물 보기'**로 네이버에서 확인합니다.

> 즉, 이 도구 = "감시할 곳 + 기준가", 네이버 = "그 단지 지금 얼마".

---

## ① 입주예정 단지 목록 (`docs/index.html`)

### 화면 사용법 (휴대폰)
- **서울 / 경기** : 위쪽 큰 버튼으로 지역(시도) 선택 → 아래 구·시 목록이 바뀜
- **입주 기간** : `2026. 08월 ~ 2027. 12월` 처럼 **시작 월 ~ 끝 월**을 골라 그 구간만
- **구·시** : 강남구, 고양시 등 세부 지역
- **예산** : 6억↓ / 9억↓ / 15억↓ 등 **분양가 기준** 필터
- **분양 / 임대**, **정렬**(입주순·분양가순·세대순 등), **검색**(단지명)
- 각 카드: **분양가**(억) → **'매물 보기'**(네이버 현재 호가) → (있으면) **분양 공고 홈페이지**

### 수정방법 ✏️
대부분 **`collect.py` 맨 위 설정 몇 줄**만 고치면 됩니다.
고친 뒤에는 [데이터 새로고침](#데이터-새로고침-수정-후-필수)을 실행해야 반영됩니다.

**(a) 보는 기간** — `collect.py`
```python
START_MONTH, END_MONTH = "2026-08", "2027-12"   # 예: 2028년까지면 END_MONTH="2028-12"
```
(범위를 넓히면 단지가 많아져 수집 시간이 길어집니다.)

**(b) 지역** — `collect.py`
```python
TARGET_SIDO = ("서울", "경기")   # 예: ("서울",) 만 / ("서울","경기","인천")
```
> 인천·부산 등도 같은 데이터에 있어 이름만 추가하면 됩니다.

**(c) 분양만 / 임대 포함** — `collect.py`
```python
INCLUDE_TYPES = ("분양", "임대")   # 분양만: ("분양",)
```

**(d) 예산 필터 구간** — `docs/index.html` 의 `BUDGETS` (만원 단위, 60000 = 6억)
```js
const BUDGETS = [
  {key:"all",   label:"전체 금액", max:Infinity},
  {key:"b6",    label:"6억↓",  max:60000},
  {key:"b9",    label:"9억↓",  max:90000},
  {key:"b15",   label:"15억↓", max:150000},
  {key:"b15up", label:"15억↑", min:150000, max:Infinity},
];
```

### 데이터 새로고침 (수정 후 필수)
`collect.py`를 고쳤거나 최신 분양가로 갱신하려면:
```bash
python collect.py        # docs/data.json 을 새로 만듦 (몇 분 소요)
git add -A && git commit -m "데이터 갱신" && git push
```
> **화면(index.html)만** 고쳤다면 `collect.py`는 안 돌려도 됩니다.
> `data.json`은 그대로 두고 커밋·push만 하면 반영됩니다.

---

## ② 매매·전세 동향 추적기 (`docs/watch.html`)

관심 단지의 **34평 이하 매매·전세 매물을 매일 수집**해, 어제 대비 가격 변동·신규·사라진
호실을 **표 + 그래프**로 봅니다.

- 상단 **단지 선택 드롭다운**(여러 단지) + **[전세]/[매매] 토글**
- **매매 탭**: **프리미엄(P) = 매매호가 − 분양가** 표시. 음수면 **마피**(분양가보다 싼 매물)로 강조.
  표는 프리미엄 낮은순 정렬, 프리미엄 추이 그래프 별도 표시.
  (분양가는 `docs/data.json`에서 자동 매칭 → **먼저 `python collect.py`를 한 번** 돌려둔 상태여야 함.
   완공 단지처럼 분양가가 없으면 프리미엄은 자동으로 숨겨집니다.)

### 최초 1회 설치
```bash
pip install playwright
python -m playwright install chromium     # 브라우저(약 150MB) 다운로드
```

### 실행 — 방법 A: 직접 (추천, 부담 없음)
매일 아침(아무 때나) **`watch/daily.bat` 더블클릭** 한 번이면 끝:
```
수집 → docs/watch_data.json 갱신 → git commit → git push  (전부 자동)
```
손으로 하려면:
```bash
python watch/scrape.py
git add docs/watch_data.json && git commit -m "동향" && git push
```
**알아두면 좋은 점**
- **정확히 아침이 아니어도** 됩니다. 하루 중 아무 때나, **하루 한 번**이면 충분.
- **하루 걸러도** 괜찮습니다. 빠진 날은 그래프에 점 하나 없을 뿐, 다음 실행부터 이어짐.
- 같은 날 **여러 번 실행 = 그날치 덮어쓰기**(중복 안 쌓임).
- `daily.bat`의 push는 그 PC에 git 로그인이 돼 있어야 합니다. push가 실패해도
  **수집은 이미 됐으니** 나중에 직접 push만 하면 됩니다.

### 실행 — 방법 B: 자동 (Windows 작업 스케줄러, 선택)
1. **작업 스케줄러** → **기본 작업 만들기**
2. 트리거: **매일**, 원하는 시각(예: 오전 9시)
3. 동작: **프로그램 시작** → `watch\daily.bat`

| 방법 | 필요 조건 |
|---|---|
| **A. 직접 실행** | 하루 한 번 `daily.bat` 실행 (PC 켤 때 아무 때나) |
| B. 작업 스케줄러 | 지정 시각에 PC가 켜져 있어야 함 (꺼져 있으면 그날 스킵) |

> 클라우드·GitHub Actions로는 **안 됩니다** — 서버 IP는 네이버가 차단합니다. **내 PC에서** 실행.

### 단지 추가/변경
`watch/scrape.py` 위쪽 `COMPLEXES` 목록에 넣으면 화면 드롭다운에 자동으로 추가됩니다.
```python
COMPLEXES = [
    {"no": "186117", "name": "힐스테이트 메디알레"},
    {"no": "119275", "name": "녹번역 e편한세상 캐슬"},
    # {"no": "새단지번호", "name": "단지이름"},   ← 이렇게 추가
]
MAX_PYEONG = 34.9              # 34평 이하 (모든 단지 공통)
```
- **단지번호 찾기**: m.land.naver.com 에서 단지 검색 → 주소 `.../complex/info/` 뒤 숫자.

### 주의 (추적기 전용)
- **네이버 이용약관상 자동수집은 회색지대**입니다. 개인이 관심단지를 **하루 1회** 보는
  저빈도 용도로만 쓰세요. 자주 돌리면 차단될 수 있습니다.
- 네이버가 화면 구조를 바꾸면 수집이 멈출 수 있습니다(그때 알려주시면 고칩니다).
- 매일 쌓일수록 그래프 추이가 뚜렷해집니다(첫날은 점 하나).

---

## ③ 8억 레이더 (`docs/radar.html`)

**답하려는 질문**: "서울에서 **8억대**로 살 수 있는 **10년 내외** 아파트가 아직 있나?
경매로 나온 건 없나?"

세 갈래로 같은 질문을 던지고, 한 화면에서 탭으로 비교합니다.

| 탭 | 출처 | 보는 숫자 | 성격 |
|---|---|---|---|
| **실거래** | 국토교통부 아파트 매매 실거래가 (공식 API) | **체결가** | "여기는 실제로 8억대에 팔렸다"는 **사실** |
| **경매** | 대법원 법원경매정보 | **최저매각가** | 이 값부터 입찰 시작 |
| **공매** | 온비드(캠코) 압류재산 | **최저입찰가** | 물건 수는 적지만 부담이 가벼운 편 |

- **경매·공매는 어제 없던 물건에 `신규` 배지**가 붙습니다. 이게 이 도구의 핵심입니다.
  (매일 돌려야 의미가 생깁니다. 첫날은 비교 대상이 없어 배지가 없습니다.)
- 매각기일이 가까우면 `D-3` 같은 배지가, 유찰된 물건은 `유찰 2회` 배지가 붙습니다.
- 각 카드의 **'단지 시세'**(네이버) · **'지도'** · **'법원 사건검색'** 으로 바로 확인.

### 최초 1회: API 활용신청 (실거래·공매 탭에 필요)

경매 탭은 **아무 설정 없이 바로** 됩니다. 실거래·공매 탭은 공공데이터 API 키가
그 서비스에 등록돼 있어야 합니다. `secret_key.txt`의 **기존 키 그대로** 쓰면 되고,
아래 두 곳에서 **활용신청 버튼만 누르면 즉시 승인**됩니다(무료).

1. [국토교통부_아파트 매매 실거래가 자료](https://www.data.go.kr/data/15126469/openapi.do) → **활용신청**
2. [한국자산관리공사_**차세대 온비드 부동산 물건목록** 조회서비스](https://www.data.go.kr/data/15157207/openapi.do) → **활용신청**

> ②는 이름이 비슷한 게 여러 개라 헷갈리기 쉽습니다. **"부동산 <u>물건목록</u>"** (15157207)이
> 맞습니다 — "부동산 <u>물건상세</u>"(15157247)는 물건관리번호를 하나씩 넣어 조회하는
> 상세용이라 목록을 못 받아옵니다. "물건상세 입찰정보"·"입찰결과목록"도 아닙니다.
> (구 API `온비드 물건 정보 조회서비스`(15000837)는 폐지돼 차세대로 대체됐습니다.)

신청 전에는 화면 위에 노란 안내가 뜨고 그 탭만 비어 있습니다(경매 탭은 정상 동작).
승인 반영까지 몇 분 걸릴 수 있습니다.

### 실행

```bash
python radar.py              # 전부 (실거래 스캔 때문에 3~5분)
python radar.py --no-deals   # 경매·공매만 (몇 초)
```
또는 **`radar.bat` 더블클릭** — 수집 → 커밋 → push 까지 자동.

- 하루 1회면 충분합니다. 하루 걸러도 됩니다(그날 신규 표시만 못 볼 뿐).
- **경매 수집은 법원 사이트에 하루 10번 남짓 요청**하는 수준이라 부담이 없습니다.
- 실거래는 서울 25개 구 × 12개월 = 약 300번 호출합니다(공식 API, 일 10,000건 한도).

### 수정방법 ✏️ — `radar.py` 맨 위 설정

```python
SCAN_MONTHS = 12                                   # 실거래 몇 개월치를 볼지
BUILD_YEAR_MIN, BUILD_YEAR_MAX = 2014, 2021        # 준공 연도 = '10년 내외'
PRICE_MIN, PRICE_MAX = 75_000, 90_000              # 만원 (7.5억 ~ 9.0억)
AREA_MIN = 40.0                                    # 전용 40㎡ 미만(원룸형) 제외
AUCTION_MONTHS_AHEAD = 6                           # 경매 매각기일을 몇 개월 앞까지
```

> 화면에서도 **예산·전용면적·지역**을 바로 바꿀 수 있습니다(수집 다시 안 해도 됨).
> `radar.py`의 값은 **수집 단계에서 걸러내는 기준**이라, 여기서 좁히면 화면에서
> 다시 넓혀도 안 나옵니다. 넉넉하게 두고 화면에서 좁히는 쪽을 권합니다.
> 단, **경매·공매는 서울 아파트 전량을 담아두므로** 화면에서 얼마든지 넓힐 수 있습니다.

### 주의 (레이더 전용)

- **경매는 공식 오픈 API가 없어** 법원경매정보 사이트가 화면에서 쓰는 내부 API를
  그대로 호출합니다. 법원이 사이트를 개편하면 멈출 수 있습니다(그때 알려주세요).
- **실거래가는 이미 체결된 값**입니다. 지금 그 값에 살 수 있다는 뜻은 아니고,
  "이 단지는 이 가격대"라는 기준선입니다. 현재 호가는 카드의 네이버 링크로 확인하세요.
- **경매·공매는 권리분석·명도·체납관리비 등 별도 위험**이 있습니다.
  카드의 ⚠ 비고(대지권 미등기, 지분매각, 선순위 임차인 등)를 반드시 읽고,
  입찰 전 **등기부·매각물건명세서·현황조사서**를 직접 확인하세요.

---

## 가족에게 공유하기 (GitHub Pages)

1. `git push` 로 저장소에 올림
2. GitHub **Settings → Pages → Branch: main / 폴더: /docs → Save**
3. 생기는 주소를 가족에게 전달 → 휴대폰에서 열람
   - 입주예정 목록: `https://<아이디>.github.io/<저장소>/`
   - 동향 추적기: `https://<아이디>.github.io/<저장소>/watch.html`
   - 8억 레이더: `https://<아이디>.github.io/<저장소>/radar.html`

---

## 공통 주의사항

- **`secret_key.txt`(공공데이터 API 키)는 절대 커밋/공유 금지** — `.gitignore`로 자동 제외됩니다.
  이 키는 `python collect.py`(분양가 수집) 실행 시 **내 PC에서만** 쓰이며, 웹페이지엔 올라가지 않습니다.
- 참고용 도구입니다. **실제 계약 전 반드시 입주자모집공고·현장·등기부를 직접 확인**하세요.
- 분양가는 청약홈 공식 데이터, 현재 호가는 네이버의 실시간 정보를 확인하세요.
