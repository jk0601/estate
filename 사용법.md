# 사용법 · 수정방법

수도권(서울·경기) 입주예정 단지를 한눈에 보는 도구입니다.
아래는 **화면 사용법**과 **직접 고치는 방법**입니다.

---

## 0. 네이버 부동산과 뭐가 다른가요?

- **네이버 부동산**: 단지 이름을 이미 알아야 검색됩니다. "앞으로 어디에 입주하는지" 목록은 없습니다.
- **이 도구**: 앞으로 수도권에 입주하는 단지 **목록 + 원래 분양가**를 자동으로 모아
  **"어디를 봐야 할지"**를 알려줍니다. 실제 현재 전세·매매 호가는 각 카드의
  **'매물 보기'**를 눌러 네이버에서 확인합니다.

> 즉, 이 도구 = "감시할 곳 + 기준가", 네이버 = "그 단지 지금 얼마".

---

## 1. 화면 사용법 (휴대폰)

- **서울 / 경기** : 위쪽 큰 버튼으로 지역(시도)을 고릅니다. 고르면 아래 구·시 목록이 바뀝니다.
- **입주 기간** : `2026. 08월 ~ 2027. 12월` 처럼 **시작 월 ~ 끝 월**을 골라 그 구간만 봅니다.
- **구·시** : 강남구, 고양시 등 세부 지역으로 좁힙니다.
- **예산** : 6억↓ / 9억↓ / 15억↓ 등 **분양가 기준**으로 거릅니다.
- **분양 / 임대** , **정렬**(입주순·분양가순·세대순 등) , **검색**(단지명).
- 각 카드: **분양가**(억) 표시 → **'매물 보기'** 로 네이버에서 현재 호가 확인 → (있으면) **분양 공고 홈페이지**.

---

## 2. 수정방법 ✏️

대부분 **`collect.py` 맨 위 설정 몇 줄**만 고치면 됩니다.
고친 뒤에는 반드시 **아래 3번(데이터 새로고침)** 을 실행해야 화면에 반영됩니다.

### (a) 보는 기간 바꾸기
`collect.py`
```python
START_MONTH, END_MONTH = "2026-08", "2027-12"   # ← 이 두 값을 바꾸세요
```
예: 2028년까지 보고 싶으면 `END_MONTH = "2028-12"`.
(범위를 넓히면 단지가 많아져 수집 시간이 길어집니다.)

### (b) 지역 바꾸기 (서울만 / 인천 추가 등)
`collect.py`
```python
TARGET_SIDO = ("서울", "경기")   # ← 예: ("서울",) 만  /  ("서울","경기","인천")
```
> 인천·부산 등 다른 시도도 같은 데이터에 들어있어 이름만 추가하면 됩니다.

### (c) 분양만 보기 / 임대 포함
`collect.py`
```python
INCLUDE_TYPES = ("분양", "임대")   # ← 분양만: ("분양",)
```

### (d) 예산 필터 구간 바꾸기
`docs/index.html` 의 `BUDGETS` 부분 (만원 단위, 60000 = 6억)
```js
const BUDGETS = [
  {key:"all",   label:"전체 금액", max:Infinity},
  {key:"b6",    label:"6억↓",  max:60000},
  {key:"b9",    label:"9억↓",  max:90000},
  {key:"b15",   label:"15억↓", max:150000},
  {key:"b15up", label:"15억↑", min:150000, max:Infinity},
];
```

---

## 3. 데이터 새로고침 (수정 후 필수)

`collect.py`를 고쳤거나, 최신 매물/분양가로 갱신하려면:

```bash
python collect.py        # docs/data.json 을 새로 만듦 (몇 분 소요)
```

그다음 GitHub에 반영:
```bash
git add -A
git commit -m "데이터 갱신"
git push
```

> **화면(index.html)만** 고쳤다면 `collect.py`는 안 돌려도 됩니다.
> `data.json`은 그대로 두고 커밋·push만 하면 반영됩니다.

---

## 4. 가족에게 공유 (GitHub Pages)

1. `git push` 로 저장소에 올림
2. GitHub **Settings → Pages → Branch: main / 폴더: /docs → Save**
3. 생기는 `https://<아이디>.github.io/<저장소>/` 주소를 가족에게 전달 → 휴대폰에서 열람

---

## ★ 전세 동향 추적기 (힐스테이트 메디알레 34평 이하)

특정 단지의 **34평 이하 전세 매물을 매일 수집**해, 어제 대비 가격 변동·신규·사라진
호실을 **표 + 그래프**로 봅니다. → 화면: `docs/watch.html`

### 최초 1회 설치
```bash
pip install playwright
python -m playwright install chromium     # 브라우저(약 150MB) 다운로드
```

### 매일 실행
```bash
python watch/scrape.py     # docs/watch_data.json 에 오늘치 누적 (1~2분)
```
그다음 `git add docs/watch_data.json && git commit -m "동향" && git push`.
→ 또는 **`watch/daily.bat` 하나로** 수집+커밋+push가 됩니다.

### 매일 자동 실행 (Windows 작업 스케줄러)
1. **작업 스케줄러** 열기 → **기본 작업 만들기**
2. 트리거: **매일**, 원하는 시각(예: 오전 9시)
3. 동작: **프로그램 시작** → `watch\daily.bat` 선택
> ⚠️ 그 시각에 **PC가 켜져 있어야** 실행됩니다. (클라우드/GitHub로는 네이버가 차단해 불가)

### 다른 단지/평형으로 바꾸기
`watch/scrape.py` 위쪽:
```python
COMPLEX_NO   = "186117"          # 네이버 단지번호 (m.land 검색 시 주소의 숫자)
COMPLEX_NAME = "힐스테이트 메디알레"
MAX_PYEONG   = 34.9              # 34평 이하
```

### 주의 (전세 추적기)
- **네이버 이용약관상 자동수집은 회색지대**입니다. 개인이 관심단지 1곳을
  **하루 1회** 보는 저빈도 용도로만 쓰세요. 자주 돌리면 차단될 수 있습니다.
- 네이버가 화면 구조를 바꾸면 수집이 멈출 수 있습니다(그때 알려주시면 고칩니다).
- 매일 데이터가 쌓일수록 그래프 추이가 뚜렷해집니다(첫날은 점 하나).

---

## 5. 주의

- **`secret_key.txt`(공공데이터 API 키)는 절대 커밋/공유 금지** — `.gitignore`로 자동 제외되어 있습니다.
- 참고용 도구입니다. **실제 계약 전 반드시 입주자모집공고·현장·등기부를 직접 확인**하세요.
- 분양가는 청약홈 공식 데이터, 현재 호가는 네이버 링크의 실시간 정보를 확인하세요.
